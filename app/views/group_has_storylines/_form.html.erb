<%= form_for(@group_has_storyline) do |f| %>
  <% if @group_has_storyline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_has_storyline.errors.count, "error") %> prohibited this group_has_storyline from being saved:</h2>

      <ul>
      <% @group_has_storyline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :storylineTag_id %><br>
    <%= f.number_field :storylineTag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
