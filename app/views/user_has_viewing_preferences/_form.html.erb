<%= form_for(@user_has_viewing_preference) do |f| %>
  <% if @user_has_viewing_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_has_viewing_preference.errors.count, "error") %> prohibited this user_has_viewing_preference from being saved:</h2>

      <ul>
      <% @user_has_viewing_preference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :viewingPreference_id %><br>
    <%= f.text_field :viewingPreference_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
